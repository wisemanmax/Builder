{
  "name": "the-builder",
  "private": true,
  "version": "0.13.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write 'src/**/*.js'",
    "check": "eslint src/ && prettier --check 'src/**/*.js'"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "prettier": "^3.2.0",
    "vite": "^6.3.5"
  }
}
